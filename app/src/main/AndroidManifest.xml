<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ursus.tracer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".TracerApplication"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.ursus.media.MediaApplication"
            android:value="application_module"/>

        <activity android:name=".MainActivity">

        </activity>

        <activity android:name="com.ursus.base.developer.DeveloperActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>